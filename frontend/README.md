Notes:

Backend
Do install: npm install

Frontend
Do install: npm install

The dependencies will be installed since package.json includes all the requirements, npm install should install them all